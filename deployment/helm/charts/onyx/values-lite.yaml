# =============================================================================
# AETHERSEARCH LITE — MINIMAL DEPLOYMENT VALUES
# =============================================================================
# Minimal AetherSearch deployment: no vector database, no Redis, no model servers.
# Only PostgreSQL is required. Connectors and RAG search are disabled, but the
# core chat experience (LLM conversations, tools, user file uploads, Projects,
# Agent knowledge) still works.
#
# Usage:
#   helm install aethersearch ./deployment/helm/charts/aethersearch \
#     -f ./deployment/helm/charts/aethersearch/values-lite.yaml
#
# Or merged with your own overrides:
#   helm install aethersearch ./deployment/helm/charts/aethersearch \
#     -f ./deployment/helm/charts/aethersearch/values-lite.yaml \
#     -f my-overrides.yaml
# =============================================================================

vectorDB:
  enabled: false

vespa:
  enabled: false

redis:
  enabled: false

configMap:
  CACHE_BACKEND: "postgres"
  AUTH_BACKEND: "postgres"
  FILE_STORE_BACKEND: "postgres"
