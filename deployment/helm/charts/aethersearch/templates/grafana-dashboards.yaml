{{- if .Values.monitoring.grafana.dashboards.enabled }}
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "aethersearch.fullname" . }}-indexing-pipeline-dashboard
  labels:
    {{- include "aethersearch.labels" . | nindent 4 }}
    grafana_dashboard: "1"
  annotations:
    grafana_folder: "AetherSearch"
data:
  aethersearch-indexing-pipeline.json: |
    {{- .Files.Get "dashboards/indexing-pipeline.json" | nindent 4 }}
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "aethersearch.fullname" . }}-opensearch-search-latency-dashboard
  labels:
    {{- include "aethersearch.labels" . | nindent 4 }}
    grafana_dashboard: "1"
  annotations:
    grafana_folder: "AetherSearch"
data:
  aethersearch-opensearch-search-latency.json: |
    {{- .Files.Get "dashboards/opensearch-search-latency.json" | nindent 4 }}
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "aethersearch.fullname" . }}-redis-queues-dashboard
  labels:
    {{- include "aethersearch.labels" . | nindent 4 }}
    grafana_dashboard: "1"
  annotations:
    grafana_folder: "AetherSearch"
data:
  aethersearch-redis-queues.json: |
    {{- .Files.Get "dashboards/redis-queues.json" | nindent 4 }}
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "aethersearch.fullname" . }}-indexing-pruning-dashboard
  labels:
    {{- include "aethersearch.labels" . | nindent 4 }}
    grafana_dashboard: "1"
  annotations:
    grafana_folder: "AetherSearch"
data:
  aethersearch-indexing-pruning.json: |
    {{- .Files.Get "dashboards/indexing-pruning.json" | nindent 4 }}
{{- end }}
