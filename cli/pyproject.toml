[build-system]
requires = ["hatchling==1.29.0", "go-bin~=1.26.1", "manygo==0.2.0"]
build-backend = "hatchling.build"

[project]
name = "aethersearch-cli"
readme = "README.md"
description = "Terminal interface for chatting with your AetherSearch agent"
authors = [{ name = "AetherSearch", email = "founders@aethersearch.app" }]
requires-python = ">=3.9"
keywords = [
  "aethersearch", "cli", "chat", "ai", "enterprise-search",
]
classifiers = [
    "Programming Language :: Go",
    "License :: OSI Approved :: MIT License",
    "Operating System :: POSIX :: Linux",
    "Operating System :: MacOS",
    "Operating System :: Microsoft :: Windows",
]
dynamic = ["version"]

[project.urls]
Repository = "https://github.com/vishalm/ai-enterprise-search-chat-aethersearch"

[tool.hatch.build]
include = ["go.mod", "go.sum", "main.go", "**/*.go", "**/*.py", "README.md"]

[tool.hatch.version]
source = "code"
path = "internal/_version.py"

[tool.hatch.build.targets.wheel.hooks.custom]
path = "hatch_build.py"
binary_name = "aethersearch-cli"
tag_prefix = "cli"

[tool.uv]
managed = false
